import { motion } from "framer-motion";

export default function FireSequence({ verse }) {
  return (
    <>
      {/* 🔥 EMBER / IGNITION */}
      <motion.div
        initial={{ opacity: 0, scale: 0.95 }}
        animate={{ opacity: 1, scale: 1 }}
        transition={{ duration: 0.3 }}
        className="absolute bg-orange-400/30 blur-lg rounded"
      />

      {/* 🔥 LIFTED VERSE */}
      <motion.div
        initial={{ y: 0, scale: 1, opacity: 1 }}
        animate={{
          y: -60,
          scale: 1.05,
          opacity: 1
        }}
        transition={{
          duration: 0.8,
          ease: [0.22, 1, 0.36, 1]
        }}
        className="text-white text-xl font-serif"
      >
        {verse.text}
      </motion.div>

      {/* 🌊 GLASS PANEL */}
      <motion.div
        initial={{ opacity: 0, x: 100 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ delay: 0.8, duration: 0.6 }}
        className="
          absolute right-[10%]
          w-[420px]
          backdrop-blur-xl
          bg-white/10
          border border-white/20
          rounded-xl
          p-6
          text-white
        "
      >
        <p>
          This path has been lived in quiet surrender.
        </p>

        <p className="mt-3 text-sm italic">
          — St Francis
        </p>
      </motion.div>
    </>
  );
}
